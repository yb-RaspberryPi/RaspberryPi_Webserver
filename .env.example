# 이 파일을 .env 로 복사한 뒤 본인 값으로 고쳐 쓸 것
# .env 는 .gitignore 에 있어서 깃허브에 올라가지 않음

# 내 Flask 포트 (student1 = 5001, student2 = 5002)
PORT=5001

# 데이터베이스 접속 정보
DB_HOST=localhost
DB_USER=appuser
DB_PASSWORD=q1w2e3
DB_NAME=webclass_s1
